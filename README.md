# Personal Portfolio (Vite + React + Express + MongoDB)

This workspace contains a full-stack portfolio scaffold:

- `client/` — Vite + React frontend
- `server/` — Express + MongoDB backend

See the README sections and run instructions in the root `package.json` scripts.
